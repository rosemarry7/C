strlen()函数（在string.h头文件中定义）可以用于获得一个字符串的长度，（不包括表示终止的空字符）
scanf()函数，在使用%s说明符时，可以用于读取包含一个单词的字符串。

C编译器在源代码程序中搜索预处理器指令，并在程序开始编译之前处理他们。include指令使处理器把另一个文件
的内容添加到文件中该指令所在的位置。

使用#define指令可以创建明显常量，也就是代表常量的符号。limits.h 和 float.h头文件使用#define定义了一套
表示整数和浮点类型的各个属性的常量。

也可以使用const修饰符来创建符号常量。

